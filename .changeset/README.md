# Changesets

This folder is managed by `@changesets/cli`.

- Add release notes with `npx changeset add`
- Stable releases are published from `main`
- RC releases are published from `next` using tag `next`
